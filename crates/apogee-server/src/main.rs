//! Apogee headless simulation server.

fn main() {
    tracing_subscriber::fmt::init();

    tracing::info!("Apogee server starting (stub)");
    tracing::info!("Phase 0 scaffold — no simulation logic yet");
}
